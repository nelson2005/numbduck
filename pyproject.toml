[build-system]
requires = ["setuptools~=75.3.0", "wheel~=0.45.0"]
build-backend = "setuptools.build_meta"

[tool.setuptools.packages.find]
where = ["."]
include = ["numbduck*"]

[project]
name = "numbduck"
dependencies = [
    "duckdb>=1.3.2,<1.6",
    "numbox>=0.5.13"
]
dynamic = ["version"]
requires-python = ">=3.10"
license = {file = "LICENSE"}
keywords = ["duckdb", "numba", "numpy"]
authors = [
  {name = "NumbDuck GitHub Repository Contributors"}
]
readme = "README.md"

[tool.pytest.ini_options]
addopts = "-m 'not benchmark'"
markers = [
    "benchmark: slow benchmarks; deselected by default, run explicitly with -m benchmark",
]

[tool.setuptools.dynamic]
version = {attr = "numbduck.__version__"}
