{
  "findingId": "TST-MEM-1",
  "verdict": "confirmed",
  "correctedSeverity": "medium",
  "reasoning": "Verified against test/test_ducklib.py:172-230, numbduck/duckdb_utils.py:36-39, numbduck/ducklib.py:165,172,775-778,943-946, and duckdb.h (duckdb_result struct at line ~452-454; duckdb_query/duckdb_destroy_result docs at lines 923-943). aux_get_data_vector (172-182) computes duckdb_result = tuple(out_result) at line 174 and returns (duckdb_result, data_chunk_p, duckdb_database, duckdb_connection) at line 182 -- out_result itself (the numpy int64[6] buffer from create_duckdb_result(), i.e. allocate_buffer(6) in duckdb_utils.py:37-39) is not returned and becomes unreachable once the function returns. This matters because duckdb_fetch_chunk is bound as intp(duckdb_result_ty) via _call_lib_func_byval (ducklib.py:172,943-946) -- it consumes the struct BY VALUE (a copied tuple works fine for that call) -- whereas duckdb_destroy_result is bound as void(intp) (ducklib.py:165,775-778) and requires an actual pointer (out_result.ctypes.data) to a live buffer holding the struct bytes. The four consumers confirmed by direct read of the file -- test_duckdb_data_chunk_get_column_count (185-189), test_duckdb_data_chunk_get_size (192-196), test_duckdb_destroy_data_chunk (199-209), test_duckdb_fetch_chunk_data_chunk_get_vector_get_data_vector (222-230) -- only receive the tuple and call aux_close_db; none reconstructs a fresh out_result buffer from the tuple values (e.g. numpy.array(duckdb_result, dtype=numpy.int64)) nor calls duckdb_destroy_result, so the duckdb_result from aux_query_1 is never destroyed in any of these four tests. duckdb.h explicitly documents 'duckdb_destroy_result must be called on the result object' (even on query failure) and gives no indication that duckdb_close/duckdb_disconnect (called via aux_close_db) frees outstanding un-destroyed results; the struct is a flat POD (deprecated fields + a void *internal_data) owned independently of the connection, so nothing else reclaims it in this test flow. One nuance in the finding's phrasing: out_result is not literally 'the only handle that can ever exist' in a hypothetical sense -- since duckdb_result is POD, a fresh buffer rebuilt from the tuple's field values (preserving internal_data) would work equally well for destroy_result -- but the code as written performs no such reconstruction anywhere, so in the actual current tree the practical claim holds: no caller of aux_get_data_vector can reach duckdb_destroy_result, and these four tests leak a DuckDB result each. This is a real, correctly localized test-hygiene/resource-leak defect, not a false positive. Severity: this is test-only code (each :memory: database + short-lived pytest process), duckdb_destroy_result itself is otherwise thoroughly exercised elsewhere in the file via the aux_execute_prepared path (18+ call sites), and the four affected tests still validate the bindings they target, so this does not indicate a production bug -- 'medium' (test coverage gap / minor per-test resource leak, not a crash or data-corruption risk) is the right severity; no correction needed."
}
