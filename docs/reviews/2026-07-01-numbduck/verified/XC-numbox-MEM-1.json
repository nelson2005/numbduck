{
  "findingId": "XC-numbox-MEM-1",
  "verdict": "confirmed",
  "correctedSeverity": "low",
  "reasoning": "Read every cited line on the current tree; all six @cfunc wrappers in examples/irr.py (109-111, 124-126, 165-167, 188-190, 209-211, 223-225) call their @njit impl with zero try/except, confirmed by direct file read. _irr_update_impl (irr.py:156-158) and _irr_combine_impl (irr.py:178-181) each hold a live borrow_structref incref (numbox/utils/meminfo.py:131-140) across a separate compiled-function call (vector_push / vector_extend, numbox/core/vector/vector.py:120-141) whose growth path allocates via numpy.empty (vector.py:123 and :137) -- exactly the 'held live across a separate call that raises' precondition for numba's documented cfunc-boundary NRT leak. This mechanism is not hearsay: numbox's own sibling UDAF path (numbox/core/bindings/_sqlite_udf_helpers.py, module docstring lines 11-19) explicitly documents that an unguarded numba @cfunc 'SWALLOWS the exception (prints Exception ignored and returns the zero default without unwinding)' and that the in-body try/except is required so 'numba can run the borrowed state's reference-count decrement, which the unwind would otherwise skip -- a per-group meminfo leak' -- and this exact failure mode is empirically exercised by passing tests in numbox/test/core/test_sqlite_udf_helpers.py (test_step_exception_surfaces_error_and_no_leak, test_finalize_exception_surfaces_error_and_no_leak, etc.), which assert alloc==free only for the GUARDED callbacks. numbduck's examples/irr.py uses the identical primitives (borrow_structref, Float64Vector/vector_push/vector_extend from the same numbox modules) but omits the guard numbox itself already established as necessary for this exact pattern. duckdb C API confirmed (ducklib.py) to expose duckdb_scalar_function_set_error but no aggregate-function error-signalling channel, consistent with the finding noting DuckDB aggregates have no sqlite3_result_error analogue. All file:line citations (irr.py cfunc ranges, meminfo.py:131-140, vector.py:123/137, irr.py:372-377 NRT check) verified exact against the current tree. The claim that _irr_state_size_impl's body 'cannot actually raise' is also correct (it is a constant return, numpy.uint64(8)). Severity: low is appropriate -- the leak requires an actual allocation failure (OOM) or a future edit introducing a raising call inside a held borrow; for realistic non-OOM inputs the ladder stays balanced, matching the review's own 'latent robustness gap, not an active miscount' characterization. This is demo/example code (examples/irr.py), not the numbox core library, which further supports low over medium/high, though the gap is real and the fix pattern is cheap (numbox already has the template)."
}
