{
  "findingId": "EX-os-MEM-1",
  "verdict": "confirmed",
  "correctedSeverity": "medium",
  "reasoning": "Read examples/online_scoring.py:150-162 (score_jit) on the current tree: conn_ptr=extract_connection_ptr(conn); stmt=create_duckdb_prepared_statement(); rc=ducklib.duckdb_prepare(...) at line 155; assert rc == ducklib.DuckDBSuccess at line 156; _score_jit_loop(...) at line 160; ducklib.duckdb_destroy_prepare(stmt.ctypes.data) at line 161 only reached on the straight-line success path -- no try/finally anywhere in the function. duckdb.h (cached copy, lines 1573-1574, directly above the duckdb_prepare declaration) states verbatim: 'Note that after calling duckdb_prepare, the prepared statement should always be destroyed using duckdb_destroy_prepare, even if the prepare fails.' This confirms leak path (a): if duckdb_prepare fails, the assert raises AssertionError before line 161 runs, and per duckdb.h the prepared-statement object (which also owns the error message per duckdb_prepare_error's docs) is never freed. Leak path (b) is also real: if _score_jit_loop (an @njit(nogil=True) function) raises for any reason, the exception propagates past line 161, again skipping the destroy call. Checked numbduck/duckdb_utils.py:25-27 (create_duckdb_prepared_statement) -- it is a bare allocate_buffer(1) with no compensating cleanup registered elsewhere (no context manager, no atexit, no finally in any caller), so nothing else in the codebase closes this gap. Line numbers in the finding (150-162 range, prepare at 155, assert at 156, destroy at 161) match the current file exactly. The reviewer's characterization is accurate and not refuted by any contract-is-satisfied-elsewhere explanation. Severity: this is example/benchmark code, not a core library binding, and under the example's own fixed, valid SQL and always-populated features table, prepare essentially always succeeds in practice, which caps real-world blast radius. But score_jit is explicitly documented as 'the canonical prepared-execute reading pattern' and is invoked repeatedly (once per worker per T in run_scaling_block), so a reader copying this pattern into production code would reproduce a genuine, documented-in-duckdb.h resource leak on any prepare failure or loop exception. Medium is a reasonable, non-inflated severity for a clear, doc-confirmed resource-leak/error-path defect in reference/teaching code; no basis to downgrade to low or escalate to high given the low practical trigger probability in this specific script."
}
