{
  "findingId": "UTL-DES-5",
  "verdict": "confirmed",
  "correctedSeverity": "medium",
  "reasoning": "Confirmed on the current tree. numbduck/utils.py load_duckdb() (numbduck/utils.py:113-130) loads the wheel's bundled shared lib at line 115 via load_lib_path (numbox/core/bindings/utils.py:167-183, RTLD_GLOBAL on Darwin), and on macOS with a symbol-less wheel (duckdb 1.4.1-1.5.2 per the :118 comment) falls back to a standalone/downloaded dylib at :121, also RTLD_GLOBAL. Because dlopen dedupes by path, the :115 load of the wheel's own file is a no-op refcount bump (same runtime the Python `duckdb` module already uses) -- but the :121 standalone load is a genuinely different file/mapping, so in the fallback branch two independent DuckDB runtimes really are resident with their own statics (macOS two-level namespace keeps each dylib's own copies rather than interposing, so 'duplicated global state' is accurate). This is not theoretical: numbduck/pybridge.py:extract_connection_ptr() reads the raw Connection* out of a `duckdb.connect()` object (created by the wheel's runtime) via pybind11-layout offsets and hands it straight into ducklib.duckdb_query (bound to whatever load_duckdb() returned -- the standalone lib in the fallback case). test/test_ducklib.py:2562-2599 and :2619+ (test_hybrid_jit_udf_on_python_connection, test_jit_udf_vs_python_udf) exercise exactly this: a Python-created connection is bridged into ducklib, a JIT UDF is registered on it, then `conn.execute()` runs back through the Python/wheel side -- a direct cross-runtime handle crossing. numbduck/utils.py itself has no comment, guard, or version check for this seam; the only mitigation is pybridge.py's live 'SELECT 1' smoke test (numbduck/pybridge.py:64-72), which is strictly weaker than numbox's own established precedent for this identical bug class -- numbox/utils/pysqlite_bridge.py's libraries_coordinated() version-checks before ever handing off a raw sqlite3* across a macOS shared-cache/multi-instance boundary (documented in /home/erik/projects/numbox/CLAUDE.md 'LLVM symbol resolution and macOS' / 'detect-and-refuse'). numbduck has no analogous duckdb-version coordination check between the wheel's duckdb.__version__ and whatever load_duckdb() bound to (the homebrew search paths in particular are unversioned, :23-26). Additionally, CI (.github/workflows/numbduck_ci.yml) runs only ubuntu-latest/ubuntu-24.04-arm/windows-latest -- macOS is never exercised, so this exact path (including duckdb-version 1.5.1, which is in CI's matrix and in the affected 1.4.1-1.5.2 range) is untested. Severity: medium is appropriate, not higher -- the defect only manifests on a narrow platform+version combination (macOS, wheel missing C API symbols, and a homebrew/downloaded standalone lib genuinely distinct from the wheel's own file) combined with actual use of the Python-connection bridge; most calls that stay entirely within ducklib's own C API (open/connect/query/destroy all via ducklib) never cross runtimes even in the fallback case."
}
