{
  "findingId": "PBR-COR-2",
  "verdict": "confirmed",
  "correctedSeverity": "medium",
  "reasoning": "Empirically reproduced on duckdb 1.3.2 / Linux x86-64 (the pinned/validated platform). Reading id(conn)+16 (cpp_obj_p, pybridge.py:59) and then cpp_obj_p+32 (conn_ptr, pybridge.py:62) with ctypes.c_void_p.from_address(...).value: on an open connection both reads are non-null and extract_connection_ptr returns a valid pointer. After conn.close(), the +16 read (cpp_obj_p) stays non-null but the +32 read (conn_ptr) becomes None, confirming the unique_ptr<Connection> reset claim. Calling numbduck.pybridge.extract_connection_ptr(conn) on the closed connection (which passes conn_ptr straight into ducklib.duckdb_query at line 67, before any caller-side use) raised: TypeError: No matching definition for argument type(s) none, int64, int64 -- a numba dispatch typing error, not the documented TypeError('expected duckdb.DuckDBPyConnection...') or RuntimeError('extracted connection pointer failed validation'). This directly confirms scenario (b) in the finding: the function's own docstring promises only TypeError (wrong conn type) or RuntimeError (failed validation query), but a closed connection breaks that contract with an unrelated, confusing numba TypeError instead of a clean guard. (Scenario (a), a null cpp_obj_p causing 'None + 32' TypeError, was not independently reproduced -- the +16 pybind11 instance pointer is set at construction and not realistically nullable via close(); it remains a secondary, more speculative point in the description and does not change the verdict.) numbox's sibling numbox/utils/pysqlite_bridge.py:168-170 has the exact analogous guard ('if db_ptr is None: raise RuntimeError(...)') that pybridge.py lacks, confirming this is a known, deliberate defensive pattern in this codebase that was not carried over. Severity medium is appropriate: the observed failure mode is a confusing/undocumented TypeError (contract violation, poor caller experience for a foreseeable use-after-close call), not a demonstrated memory-safety crash -- numba's strict signature typing rejected the None argument before any native call occurred in this reproduction, so 'UB/crash' in the finding text is speculative/alternative phrasing rather than the observed outcome. Recommendation to add an explicit `if not conn_ptr: raise RuntimeError(...)` (and similarly guard cpp_obj_p) after each ctypes read is sound and directly mirrors the numbox precedent."
}
