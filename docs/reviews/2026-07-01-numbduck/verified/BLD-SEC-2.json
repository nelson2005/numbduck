{
  "findingId": "BLD-SEC-2",
  "verdict": "refuted",
  "correctedSeverity": "none",
  "reasoning": "The cited code exists exactly as described: numbduck_ci.yml:56 `echo \"VERSION=${{ github.ref_name }}\" >> $GITHUB_ENV` (inside the `if [ \"${{ github.ref_type }}\" == \"tag\" ]` branch at line 55) and line 63 `echo \"__version__ = '${{ env.VERSION }}'\" > numbduck/__init__.py` are textually present and use the same unsafe direct-interpolation-into-run:-text pattern as release.yml's BLD-SEC-1. However, the finding's failure_scenario -- 'A maintainer pushes tag `v2\\`id>/tmp/x\\``; at line 56 the backtick command substitution runs during the Determine version step in every matrix job' -- is not achievable on the current tree, because this specific workflow's triggers make the vulnerable branch dead code. numbduck_ci.yml:3-7 declares only `on: push: branches: [\"**\"]` and `on: pull_request: branches: [\"main\"]` -- there is no `tags:` or `tags-ignore:` key anywhere in the file. GitHub's documented behavior (docs.github.com/en/actions/reference/workflow-syntax-for-github-actions, on.push.<branches|tags> section) states verbatim: 'If you define only tags/tags-ignore or only branches/branches-ignore, the workflow won't run for events affecting the undefined Git ref' -- confirmed independently via two separate fetches of GitHub's official docs. Because numbduck_ci.yml defines only a branches filter and no tags filter, pushing a git tag does NOT trigger this workflow at all; the push event is simply not evaluated/dispatched for tag refs. For the workflow's other trigger, pull_request, github.ref_type is always 'branch' (a PR ref is never a tag). Therefore `github.ref_type == \"tag\"` can never be true for any run of numbduck_ci.yml under its current trigger configuration, the injection sink at line 56/63 is unreachable, and the 'Determine version' step always falls into the safe synthetic-version else-branch (`BUILD_VERSION=\"0.1.${{ github.run_number }}\"`, where run_number is a GitHub-controlled integer, not attacker-influenced). This is a materially different situation from release.yml (BLD-SEC-1), which triggers on `release: types: [published]` -- a release IS associated with a real tag, so `github.ref_type` genuinely is 'tag' there and that sink is live; BLD-SEC-1 remains a valid, reachable finding. The code pattern in numbduck_ci.yml is still a latent anti-pattern (identical to release.yml's, presumably copy-pasted) and would become exploitable if a `tags:` filter were ever added to this workflow's `on.push` block without also fixing the interpolation -- worth a defensive cleanup for consistency with the release.yml fix -- but as characterized, the finding asserts an active, exploitable script-injection vulnerability reachable by 'a maintainer pushing a tag', and that specific attack is not possible given the workflow's actual trigger configuration. The dead-code status is not mentioned or accounted for anywhere in the finding's reasoning about reduced severity (which cites only reduced permissions/no-publish, not unreachability). Downgrading to 'none' (dormant/latent code-quality nit, not an active vulnerability) rather than confirming the claimed 'low' severity live exploit."
}
