{
  "findingId": "CFG-COR-2",
  "verdict": "confirmed",
  "correctedSeverity": "medium",
  "reasoning": "Reproduced the exact failure mechanism directly against numba (numbox's venv, numba installed there): njit(sig, cache=True, foobar=1)(func) raises KeyError: \"Unrecognized options: {'foobar'}. Known options are dict_keys([...])\" -- a message that names numba's option keys but never mentions NUMBDUCK_JIT_OPTIONS or configurations.py. Confirmed the call chain on the current tree: numbduck/configurations.py:19 `jit_options = get_jit_options()` runs at import with zero key validation (only json.loads + isinstance-free passthrough, per configurations.py:5-16); numbduck/ducklib.py:8 imports that dict at module top (`from numbduck.configurations import jit_options`); the first consumer is the module-level decorator `@proxy(signatures.get(\"duckdb_array_type_array_size\"), jit_options=jit_options)` at ducklib.py:276, which executes immediately during ducklib's own import (decorators run at class/module-body execution, not lazily). Inside that call, numbox/core/proxy/proxy.py:75 (`func_jit = njit(sig, **jit_options)(func)`) spreads the dict straight into njit with no filtering (proxy.py:69's `isinstance(...) and ... or {}` only guards non-dict values, it does not filter keys). So NUMBDUCK_JIT_OPTIONS='{\"cache\": true, \"foobar\": 1}' deterministically kills any import of numbduck bindings with the numba KeyError above, and nothing in the message or in get_jit_options() surfaces the offending env var. Severity medium is appropriate: it's self-inflicted (requires an operator to set a malformed env var) so not a default-path bug, but once triggered the blast radius is total (ducklib is the package's core import) and the error gives no hint of the root cause, matching CFG-COR-2's description exactly."
}
