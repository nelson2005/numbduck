{
  "findingId": "REC-EX-fraud-MEM-1",
  "verdict": "confirmed",
  "correctedSeverity": "low",
  "reasoning": "NOT REAL avenue fails: verified byte-for-byte against the CURRENT tree. examples/fraud_score.py:102-144 _fraud_chunk_impl reads a_amt/a_co/a_hco/a_hr/a_rt/a_rx[i] in the loop at 125-144 with zero calls to duckdb_vector_get_validity, and there is no 'NULL'/'validity' token anywhere in the file (grep confirms). Both duckdb_vector_get_validity (ducklib.py:1207-1210) and duckdb_validity_row_is_valid (ducklib.py:1189-1192) are bound and correctly used as the guarded reference pattern in examples/irr.py:140-152 (val != 0 check then duckdb_validity_row_is_valid per row), so the claim that the fix is available in-repo is also accurate. DUPLICATE avenue fails: scanned docs/reviews/2026-07-01-numbduck/reconcile/new-index.json for fraud_score.py entries -- only EX-fraud-1 (func_p handle leak on the register-failure path, findings/EX-fraud.json) and EX-scalar-MEM-1 (cfunc exception-swallow / silent partial output on raise, findings/EX-scalar-MEM.json) exist, both distinct root causes from a missing validity guard on non-raising NULL input. Read both finding files in full: EX-fraud.json's 'notes' field explicitly discusses the exact same gap ('NULL/validity handling: the kernel reads no validity masks... a safe documented assumption... not a live defect') but this is prose in a notes field, not a filed finding with an id -- it does not appear in new-index.json and cannot be cited as a covering id. The project-level convention (understanding/examples.md invariant 7 and the haversine.py analog at line 138) documents this as a deliberate cross-example choice, but that documentation lives only in the review's own understanding/ notes, not inline in fraud_score.py itself (verified via grep: no NULL/validity comment in the file), so the recommendation's ask for an inline comment (option a) is not already satisfied in-repo. Since neither refutation path holds, the candidate stands: a real, non-duplicated, low-severity gap. Severity stays low per both the original and adopted assessment -- no memory-safety issue (in-bounds full-width buffer reads), inert against the example's own dense non-NULL synthetic data, relevant only as a copied usage pattern for real nullable-column use."
}
