{
  "findingId": "PBR-SEC-1",
  "verdict": "confirmed",
  "correctedSeverity": "medium",
  "reasoning": "Verified against current tree. pybridge.py:57-62 (extract_connection_ptr) walks the duckdb wheel's own DuckDBPyConnection C++ object (id(conn)+16, then +32) to obtain a raw Connection*, then at pybridge.py:67-68 passes it to ducklib.duckdb_query / ducklib.duckdb_destroy_result. Confirmed those wrappers (ducklib.py:1111-1114 for duckdb_query) route through numbox's _call_lib_func intrinsic (numbox/core/bindings/call.py), which resolves the C symbol via `ll.address_of_symbol(func_name)` -- i.e. whatever library is currently registered process-wide, not necessarily the wheel's own handle. That process-wide library is whichever one numbduck/utils.py load_duckdb() loaded via load_lib_path (RTLD_GLOBAL). Confirmed load_duckdb (numbduck/utils.py:112-124) has a real fallback chain on the wheel's stripped-C-API path: NUMBDUCK_LIBDUCKDB env override, Homebrew paths (_MACOS_LIBDUCKDB_SEARCH_PATHS), or a downloaded release zip -- none of these are checked against duckdb.__version__ at load time (duckdb.__version__ is used only for cache-directory naming/download-URL construction in utils.py:19-21/51, never compared to the actually-loaded lib's version). Grep across pybridge.py, ducklib.py, and utils.py confirms no duckdb_library_version binding and no version-coordination check exists anywhere in numbduck. This is a real gap distinct from (not fixed by) the prior review's XC-ver-2 finding, which covered the loader's lack of a version check in general -- PBR-SEC-1 is specifically about pybridge.py trusting a pointer minted by the wheel's private, non-ABI-stable pybind11/C++ object layout and handing it to whatever lib got JIT-linked, which is a more fragile contract than the public C API structs XC-ver-2 discussed. The sibling numbox/utils/pysqlite_bridge.py, read directly, confirms the claimed contrast is accurate: libraries_coordinated() (pysqlite_bridge.py:93-107) compares sqlite3_libversion() (bindings) against sqlite3.sqlite_version (Python module) and extract_connection_ptr (pysqlite_bridge.py:159-167) raises RuntimeError on mismatch rather than crossing a pointer between two libraries -- pybridge.py has no analogous check. The failure mode described (UB dereference of a Connection* from one duckdb build inside another build's duckdb_query, unrecoverable via the SELECT-1 validation since a wrong-layout deref is UB not a clean error code) is a correct read of the code. This surface is reachable only via the documented macOS/standalone fallback path (Linux normally loads the wheel's own .so and coincides), which bounds the blast radius -- consistent with rating it medium rather than high, matching the severity precedent set by the related XC-ver-2 finding for the same class of unguarded library-version mismatch."
}
