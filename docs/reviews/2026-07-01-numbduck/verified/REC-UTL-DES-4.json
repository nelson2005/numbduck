{
  "findingId": "REC-UTL-DES-4",
  "verdict": "confirmed",
  "correctedSeverity": "low",
  "reasoning": "Verified against the CURRENT tree (numbduck/utils.py, HEAD of review/numbduck-2026-06-29). _find_standalone_libduckdb (:33-36) reads NUMBDUCK_LIBDUCKDB and, if os.path.isfile() is False, silently falls through to return None with no diagnostic distinguishing 'unset' from 'set but invalid'. load_duckdb's non-Darwin branch (:124-128) then raises RuntimeError('DuckDB C API symbols not found in the Python wheel. Set NUMBDUCK_LIBDUCKDB=/path/to/libduckdb.so') without echoing the rejected path and without noting that the Darwin branch (:129, _download_libduckdb) offers an interactive-download fallback unavailable on non-Darwin. Both the message-content defect and the platform-asymmetry omission described in the candidate hold verbatim on current line numbers 124-128 / 34-36 / 129. Checked for duplication against docs/reviews/2026-07-01-numbduck/reconcile/new-index.json: the current UTL-DES set (DES-1 dedup constants, DES-2 missing symbol re-check on download path, DES-3 input() EOFError, DES-5 dual RTLD_GLOBAL runtimes, DES-6 raw urllib/zipfile errors) and UTL-COR set (COR-1 regex anchoring, COR-2 os.remove crash-on-import, COR-3 dual-runtime shadowing) all target different root causes on utils.py; none discuss the non-Darwin RuntimeError message's content, the missing path echo, or the macOS-only download asymmetry. UTL-COR-2 is adjacent (same os.remove/discovery-function critique) but is about a crash side effect, not the terminal error message's wording. No existing current finding covers this specific ergonomics defect, so it is neither refuted-as-unreal nor a duplicate. Severity 'low' matches the candidate: it's a message-quality/UX issue on an already-terminal failure path, not a functional bug.",
  "mapperJustification": "Mapper's own currentEvidence (UTL-DES.json oldId UTL-DES-4) independently confirms the same two anchors: utils.py:34-36 (env var silently discarded) and :124-128 (unconditional generic message, no echo, no mention of :129 Darwin fallback)."
}
