{
  "findingId": "BLD-DES-5",
  "verdict": "CONFIRMED",
  "correctedSeverity": "low",
  "reasoning": "All three sub-claims verified against current CURRENT-tree files. (1) Pinned-action lag: release.yml:15 uses actions/checkout@v4, release.yml:17 uses actions/setup-python@v5, release.yml:40 uses actions/upload-artifact@v4, release.yml:60 uses actions/download-artifact@v4 -- while numbduck_ci.yml:46,48 use checkout@v6/setup-python@v6, doc-codeblock-flake8.yml:12,14 use checkout@v6/setup-python@v6, link-check.yml:16,64 use checkout@v6/upload-artifact@v7, and haversine_bench.yml:17,19 use checkout@v6/setup-python@v6. release.yml is the clear outlier, several major versions behind every sibling workflow. (2) No sdist: release.yml:37 runs `python -m build --wheel` only (never `python -m build` for sdist+wheel), so the PyPI-published artifact set has no source distribution; pyproject.toml confirms this is a pure-Python package (setuptools backend, no extension modules), so the gap is real but low-impact (wheel is universally installable). (3) Inconsistent fallback minor: release.yml:27 uses `BUILD_VERSION=\"0.0.${{ github.run_number }}\"` while numbduck_ci.yml:58 uses `BUILD_VERSION=\"0.1.${{ github.run_number }}\"` -- confirmed literal mismatch in the synthetic non-tag version scheme. Bonus detail also checks out: release.yml only triggers on `release: types: [published]` (release.yml:3-5), and GitHub Release-publish events are always tag-backed, so the `else` non-tag branch (release.yml:26-28) is effectively dead code, correctly noted as an ancillary observation rather than the core claim. All findings are cosmetic/maintainability issues in packaging & CI infra with no functional, security, or binding-correctness impact, so severity=low is appropriate and not underrated or overrated."
}
