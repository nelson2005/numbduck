{
  "findingId": "XC-ver-3",
  "verdict": "confirmed",
  "correctedSeverity": "high",
  "reasoning": "The dual-runtime condition is the designed macOS fallback code path, not a theoretical edge case. utils.py:115 loads the wheel lib (RTLD_GLOBAL); _has_capi_symbols fails on macOS duckdb>=1.4.1 because CLAUDE.md confirms the wheel deliberately hides all duckdb_* via macOS -exported_symbol. utils.py:121 or :130 then loads a standalone libduckdb.dylib (Homebrew or downloaded) also RTLD_GLOBAL. These are distinct files from the DuckDB code embedded in the wheel, so the OS loads them as separate runtimes. numbox/core/bindings/call.py:72 resolves each C function address via ll.address_of_symbol() from the LLVM global symbol table; duckdb_query resolves to the standalone runtime's copy because the wheel did not contribute that symbol to the global namespace. pybridge.py:62 extracts a Connection* from a Python duckdb.DuckDBPyConnection whose Connection object was allocated by the wheel's internal DuckDB runtime, then pybridge.py:67 passes that pointer to ducklib.duckdb_query which routes into the standalone runtime. The SELECT 1 validation at pybridge.py:64-72 is itself a cross-runtime call and does not prove the pointer is valid in the standalone runtime's allocator/state context. No platform guard prevents pybridge from being used on macOS. Passing an opaque handle created by one DuckDB runtime to another independent DuckDB runtime's API is undefined behavior regardless of version match. Severity high is appropriate: the impact is crash or silent state corruption on the primary affected platform (macOS, duckdb>=1.4.1) via the normal pybridge code path."
}
