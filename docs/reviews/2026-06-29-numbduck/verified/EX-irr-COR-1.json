{
  "findingId": "EX-irr-COR-1",
  "verdict": "confirmed",
  "correctedSeverity": "medium",
  "reasoning": "The defect is real and correctly characterized. Code evidence (examples/irr.py:82-94): irr_bisect runs at most 100 iterations, exits early only via `abs(npv) < 1e-9` (line 88), and returns `math.nan` on exhaustion (line 94). The docstring (lines 14-16) documents NaN only for the empty-group case; the finalize callback confirms this at line 204. A bisection-exhaustion NaN is therefore silently indistinguishable from a documented empty-group result.\n\nBracket convergence: the initial bracket [-0.99, 10.0] has width 10.99. The ULP at r~0.0221 is ~3.47e-18, so the bracket collapses to float resolution after ~62 iterations; the remaining ~38 iterations compute the same constant r_mid and the same NPV.\n\nNPV floor analysis for 100x scale (investment=1e6, cashflow=1.3e6, period=12): The dominant error source is 12x amplification of the rounding error introduced by `1.0 + r_mid`. The addition 1.0+r_mid discards precision in r_mid below ULP(1.0)=eps~2.22e-16; that ~eps/2 relative error in the base propagates to ~12*eps/2=6*eps~1.33e-15 relative error in (1+r)^12 (value ~1.3), giving absolute error ~1.73e-15 in the power. Dividing 1,300,000 by a value that has 1.73e-15 absolute error gives error ~1,300,000*(1.73e-15/1.3^2)~1.33e-9 in the cashflow term, plus ~2.22e-10 from the division and final subtraction rounding. Total irreducible NPV error ~1.5e-9 to 1.8e-9, which exceeds the 1e-9 gate. For the original test 3 (investment=1e4): the same calculation gives floor ~1.5e-11 to 1.8e-11 << 1e-9, so the gate fires and all bundled tests pass, masking the defect.\n\nThe reviewer's claim of floor ~2.6e-9 at 100x scale is in the same order of magnitude as my calculation (~1.5-1.8e-9); both exceed 1e-9 and confirm the gate can never be satisfied at that scale. The reviewer's |dNPV/dr|*ULP(r) framing underestimates the dominant term but reaches the same conclusion.\n\nSeverity 'medium' is appropriate: examples/irr.py is the reference UDAF implementation users are expected to copy; investment above ~500k at period 12 (realistic in finance) triggers the bug; the failure is silent NaN with no diagnostic. It is not 'high' because the affected code is an example module rather than a library routine."
}
