{
  "findingId": "PBR-DES-1",
  "verdict": "confirmed",
  "correctedSeverity": "high",
  "reasoning": "The defect is real and the characterization is accurate. pybridge.py lines 58-62 apply the +16 pybind11 header offset and +32 DuckDBPyConnection member offset unconditionally with no check of duckdb.__version__. The docstring at line 31 explicitly states 'Validated on duckdb 1.3.2' and line 34 says 'Re-verify offsets when upgrading duckdb', which is a prose instruction, not a runtime guard. CLAUDE.md declares the supported range as duckdb>=1.3.2,<1.6, so any 1.4.x or 1.5.x install runs on unverified offsets.\n\nNo version guard was found anywhere in pybridge.py. utils.py does read duckdb.__version__ (line 51) and duckdb.__version__ (line 20) but only to build a versioned macOS cache path for the standalone libduckdb download — not to validate struct layout. In fact utils.py lines 97-104 and the comment at line 118 ('Python wheel missing C API symbols (macOS with duckdb >= 1.4.1)') confirm the project already handles 1.4-specific behavioral differences elsewhere, making the absence of a layout guard in pybridge.py conspicuous. ducklib.py lines 57 and 65 show similar version-sensitivity (VARINT/BIGNUM rename across <=1.4/>=1.5; DUCKDB_TYPE_TIME_NS added in 1.5+).\n\nThe SELECT 1 validation (lines 65-72) is only a partial mitigation: it catches a conn_ptr that survives duckdb_query and returns DuckDBError. If the +16 offset is wrong and cpp_obj_p is NULL, the ctypes.c_void_p.from_address at line 62 dereferences near-NULL and crashes before SELECT 1. If cpp_obj_p is non-NULL garbage and cpp_obj_p+32 yields a plausible-looking address, duckdb_query may segfault inside the C API with no Python exception. The catchable error path (RuntimeError at line 70) is only reachable for the subset of wrong pointers that happen to make duckdb_query return DuckDBError cleanly.\n\nHigh severity is correct: any user installing within the declared supported range on 1.4.x or 1.5.x gets no signal that the offsets are unvalidated, and the most likely failure mode (NULL or shifted pointer) crashes the interpreter rather than raising a catchable exception."
}
