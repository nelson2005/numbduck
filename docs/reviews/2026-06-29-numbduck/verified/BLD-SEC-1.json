{
  "findingId": "BLD-SEC-1",
  "verdict": "confirmed",
  "correctedSeverity": "medium",
  "reasoning": "The script injection pattern is real and correctly identified. numbduck_ci.yml:53 interpolates `${{ github.ref_name }}` directly into a bash run: block; GitHub Actions expands `${{ ... }}` before the shell executes, so a tag name containing shell metacharacters (which git permits: $, backtick, ;, (, ), |, &, {} are all legal in git ref names) yields arbitrary command execution. Line 60 then propagates the same tainted value into numbduck/__init__.py, giving a second execution vector via pip install / pytest import. The reachability analysis is correct: the push trigger at lines 3-5 uses `branches: [\"**\"]` with no `tags:` key, so tag pushes never start this workflow and the `ref_type == tag` branch at line 52 is currently unreachable. The live else-branch (line 55) uses `github.run_number`, a GitHub-assigned integer, which is safe. The defect is latent but trivially activated: adding `tags:` to the push trigger (the natural next step for tag-based release versioning) makes it a live RCE vector. Medium severity is appropriate — not critical because it is latent and requires a deliberate trigger-config change to become exploitable, but not low because the activation barrier is a one-line YAML edit and the impact once activated is full runner compromise plus injected Python in the built wheel."
}
