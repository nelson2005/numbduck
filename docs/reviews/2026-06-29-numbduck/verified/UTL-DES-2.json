{
  "findingId": "UTL-DES-2",
  "verdict": "confirmed",
  "correctedSeverity": "medium",
  "reasoning": "_download_libduckdb (lines 50-91) verifiably fuses four concerns with zero injectable collaborators. (a) Consent: lines 57-77 hard-code the env-var key and call builtin input() directly; the NUMBDUCK_LIBDUCKDB_DOWNLOAD=1 env var bypasses the interactive prompt for automation but does not make the consent decision injectable as a parameter or callable. (b) Network fetch: line 83 calls urlopen(url) directly with a URL built inline from duckdb.__version__ (line 51-56); neither the URL nor the opener is a parameter. (c) Unzip: lines 85-89 extract in-memory. (d) Write: lines 86-90 write to a path derived from module global _LIBDUCKDB_CACHE_DIR; dest is not a parameter. Lazy imports of io/zipfile/urlopen at lines 78-80 inside the body further obscure the dependency surface. Zero test coverage confirmed: grep of test/test_ducklib.py and test/test_init.py finds no reference to _download_libduckdb, NUMBDUCK_LIBDUCKDB_DOWNLOAD, or urlopen. The surprising RuntimeError raised on a declined prompt (lines 71-77) from a function named _download_... is accurate. The NUMBDUCK_LIBDUCKDB_DOWNLOAD env var partially mitigates the interactive-consent concern for CI but leaves the fetch+extract+write path fully untestable without monkeypatching module globals and builtins. Medium severity is appropriate: this is a design/testability concern with no immediate correctness impact, but the fallback download path is the most fragile part of the loader and cannot be exercised in unit tests."
}
