{
  "findingId": "BLD-DES-4",
  "verdict": "confirmed",
  "correctedSeverity": "medium",
  "reasoning": "All claims verified against source.\n\n1. INSTALL ASYMMETRY CONFIRMED (numbduck_ci.yml:64-67): The 'Install dependencies' step pins numba (line 65) and duckdb (line 66) to explicit matrix values, then runs `pip install -e .` (line 67) with no preceding numbox pin. pyproject.toml:13 declares `numbox~=0.5.11`, which PEP 440 resolves to >=0.5.11,<0.6.0 — any latest 0.5.x patch at install time.\n\n2. HAVERSINE_BENCH ASYMMETRY CONFIRMED (haversine_bench.yml:27-28): numpy==1.26.4, pyarrow==14.0.0, numba==0.64.0, duckdb==1.5.1 are all pinned exactly; numbox is again installed transitively via `pip install -e .` with no preceding explicit pin.\n\n3. INTERNAL API COUPLING CONFIRMED (ducklib.py:2-4): numbduck imports directly from numbox's internal call infrastructure: `_call_lib_func`, `_call_lib_func_byval` (numbox.core.bindings.call), `signatures` dict (numbox.core.bindings.signatures), `cres`/`cres_if_available` (numbox.utils.highlevel), and `load_lib_path` (numbox.core.bindings.utils). These are the exact symbols identified in the finding. A numbox 0.5.x patch that changes any of these could silently alter CI pass/fail.\n\n4. NO MITIGATING MECHANISM: No lock file, constraints file, or requirements.txt exists anywhere in the project tree.\n\nSeverity medium is correct: the defect is genuine (unpinned despite tight internal coupling), creates a real non-reproducibility risk, but requires an actual numbox patch release to trigger — not a latent security or correctness bug in the current codebase."
}
