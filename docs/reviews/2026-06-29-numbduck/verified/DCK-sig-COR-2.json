{
  "findingId": "DCK-sig-COR-2",
  "verdict": "confirmed",
  "correctedSeverity": "low",
  "reasoning": "The DuckDB C API declares `uint64_t *duckdb_vector_get_validity(duckdb_vector vector)` — a pointer return. The numbduck file's documented and practiced convention (CLAUDE.md line 97: 'numbduck uses intp for all pointers') maps every pointer to `intp`. The immediately adjacent sibling duckdb_vector_get_data (ducklib.py:235) correctly uses `intp(intp)` for its `void*` return. Line 236 instead uses `uint64(intp)`, a scalar value type, for the `uint64_t*` pointer return. This is a type-convention violation with a concrete downstream consequence: `duckdb_validity_row_is_valid` is correctly typed `int8(intp, intp)` at line 233, so in @njit/@cfunc code, passing the uint64 return of duckdb_vector_get_validity directly as the first argument produces a uint64->int64 type mismatch, which numba unifies to float64 (same float-promotion hazard documented in DCK-sig-COR-1). The workaround is visible in irr.py:140-143, where every call site wraps the return in `numpy.intp(ducklib.duckdb_vector_get_validity(...))` — an explicit cast that would be unnecessary if the return were already typed intp. Severity low is appropriate: on 64-bit targets both types are 64-bit wide, the address value is preserved intact, and there is no ABI mismatch or data corruption. The defect is type-hygiene/convention-inconsistency with a latent numba float-promotion trap in mixed-type expressions."
}
