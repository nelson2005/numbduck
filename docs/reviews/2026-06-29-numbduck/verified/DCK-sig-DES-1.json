{
  "findingId": "DCK-sig-DES-1",
  "verdict": "confirmed",
  "correctedSeverity": "medium",
  "reasoning": "Every factual claim in this finding was verified directly against ducklib.py. The bind_* wrapper run occupies lines 287-410 (21 wrappers: boolean, blob, date, int8/16/32/64, double, float, parameter_index, null, time, timestamp, timestamp_tz, uint8/16/32/64, value, varchar, varchar_length). The scalar wrapper block occupies lines 1212-1311. The aggregate block occupies lines 1314-1395. Immediately after the aggregate block, appended at EOF, are exactly the six wrappers the reviewer identifies: duckdb_scalar_function_get_extra_info (1398), duckdb_scalar_function_set_error (1404), duckdb_bind_hugeint (1410), duckdb_bind_uhugeint (1416), duckdb_bind_interval (1422), duckdb_bind_decimal (1428). None of these six appear in their family's wrapper block. The reviewer's secondary claim about the signature dict is also accurate: the four bind signatures appear correctly inside the bind sig block at lines 82-96, but the two scalar sigs (duckdb_scalar_function_get_extra_info at line 271 and duckdb_scalar_function_set_error at line 272) are appended after the aggregate sig block (256-269) rather than in the scalar sig block (238-254). There is no runtime impact — all @cres decorators look up by name and the signatures dict is complete — but the grouping invariant is violated in a way that is invisible to a scan of the bind_* run and could prompt a maintainer to add a duplicate hugeint or interval wrapper. Severity medium is correct: no functional defect, but a concrete, verifiable organization drift with a plausible duplicate-wrapper failure mode."
}
