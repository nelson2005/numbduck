{
  "findingId": "TST-cover-8",
  "verdict": "refuted",
  "correctedSeverity": "none",
  "reasoning": "The finding's central premise is factually incorrect. It claims 'all struct-by-value ABI correctness rests on round-trip asserts that run only on SysV x86-64 (Linux)' and that 'The Windows x64 branches in numbox call.py...are never driven by this suite.' The CI matrix in .github/workflows/numbduck_ci.yml (lines 20-39) explicitly includes three runner categories: ubuntu-latest (SysV x86-64), ubuntu-24.04-arm (AAPCS64), and windows-latest (Windows x64, restricted to Python 3.11 + duckdb 1.5.1). The struct bind tests (test_bind_hugeint, test_bind_uhugeint, test_bind_interval, test_bind_decimal at lines 835-906 of test_ducklib.py) and value create/get round-trips (test_create_get_hugeint, test_create_get_uhugeint, test_create_get_interval, test_create_get_decimal at lines 1263-1327) carry no platform-specific skipif marks — they run on all three CI hosts. The Windows x64 sret/by-pointer path in numbox/core/bindings/call.py and the AAPCS64 by-reference path are therefore exercised every CI run on their respective runners. The macOS loader sub-claim (no macOS CI runner, _download_libduckdb interactive branch unmocked) has technical merit but the finding itself characterizes it as 'inherent to the CI host,' and it does not concern the ABI correctness path cited in the title. The dominant claim about struct ABI coverage being Linux-only is wrong; no defect exists."
}
