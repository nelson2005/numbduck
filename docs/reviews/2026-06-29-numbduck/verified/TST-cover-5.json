{
  "findingId": "TST-cover-5",
  "verdict": "confirmed",
  "correctedSeverity": "low",
  "reasoning": "test_create_array_value (test/test_ducklib.py:1634-1647) creates values 1, 2, 3 with duckdb_create_array_value and only asserts `av != 0`, then destroys everything. The contrast with every peer container test is unambiguous: test_create_get_list_value (1531) calls duckdb_get_list_child and asserts element values 10/20; test_create_get_map_value (1563) asserts map key/value pairs; test_create_get_struct_value (1602) calls duckdb_get_struct_child and asserts values 42/99. The test function name deliberately omits 'get' (vs test_create_get_*) and no duckdb_get_array_child is bound in numbduck/ducklib.py — the signatures dict has only array *type* introspection functions (duckdb_array_type_array_size, duckdb_array_type_child_type), no array *value* element getter. This indicates the DuckDB C API for v1.3.2-1.5 does not expose a direct element accessor for array values, so the recommendation to 'mirror the list/struct tests' directly is not achievable without a new binding. However, element verification via a query (the reviewer's 'embed in a query' alternative) is viable and is the correct fix path. The core finding holds: a non-null but malformed array handle passes the test unchallenged. Severity low is appropriate — this is a weak-assertion gap, not a correctness bug in the bindings."
}
