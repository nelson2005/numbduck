{
  "findingId": "BLD-DES-2",
  "verdict": "confirmed",
  "correctedSeverity": "high",
  "reasoning": "All four claimed facts independently verified:\n\n1. pyproject.toml:26 declares the marker as 'benchmark: slow benchmarks, run only locally (deselected in CI)' — confirmed (read directly).\n\n2. No addopts in [tool.pytest.ini_options] — pyproject.toml has only a 'markers' key under [tool.pytest.ini_options], no 'addopts' key. grep for 'addopts' across the entire repo returns zero hits in source files.\n\n3. No conftest.py anywhere in the repo — `find /home/erik/projects/numbduck -name conftest.py` returned nothing. No pytest.ini, setup.cfg, or tox.ini found either.\n\n4. CI step is bare `pytest` (numbduck_ci.yml:76) — confirmed; no `-m` flag or any filtering argument.\n\n5. test_udf_benchmark (test/test_ducklib.py:2684) is decorated @pytest.mark.benchmark, hard-codes ROW_COUNTS = [10_000, 100_000, 1_000_000] at line 2703, has no env gate, and the docstring records the 1M-row Python UDF at ~118.8s — all confirmed by direct read.\n\nNo alternative mechanism could satisfy the marker's promise: pytest does not natively deselect markers without configuration, and no benchmark plugin (e.g., pytest-benchmark) appears in pyproject.toml dependencies that might alter default collection behavior. The benchmark runs in full on every matrix job, directly contradicting the marker description.\n\nSeverity 'high' is appropriate: the 1M-row Python scalar path alone is ~119s per the docstring, and this runs across a large matrix (ubuntu-latest + ubuntu-24.04-arm across 4 Python versions, 2 numba versions, 3 duckdb versions), multiplying CI cost significantly — precisely the scenario the marker comment says it prevents."
}
